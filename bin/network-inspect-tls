#!/bin/bash

for i in "$@"; do
    CERT=''

    if [ -e "${i}" ]; then
        if [ -f "${i}" ]; then
            if [ -r "${i}" ]; then
                if [ -s "${i}" ]; then
                    CERT=$(cat "${i}" 2> /dev/null)
                else
                    echo "✗ file is empty: ‘${i}’" 1>&2
                    continue
                fi
            else
                echo "✗ read permission not granted: ‘${i}’" 1>&2
                continue
            fi
        else
            echo "✗ not a regular file: ‘${i}’" 1>&2
            continue
        fi
    else
        CERT=$(openssl s_client -connect "${i}" < /dev/null 2> /dev/null)
    fi

    if [ "${CERT}" = '' ]; then
        echo "✗ unable to establish tls connection: ‘${i}’" 1>&2
        continue
    fi

    printf '\n%s\n\n' "[ ${i} ]"

    openssl x509 -noout -text -fingerprint <<< "${CERT}"
done
