#!/bin/bash

shorten_url () {
    curl --fail --location --silent --show-error --request POST --form "url=${1}" --form "code=${2}" 'https://git.io'
}

#shorten_url 'https://github.com/networkhermit' vac

curl --head 'https://git.io/vac'
